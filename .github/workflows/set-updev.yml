name: "Setup — Dev Server"
on:
  workflow_dispatch: 

jobs:
  setup:
    name: "Install Docker on Dev VM"
    runs-on: self-hosted
    steps:
      - name: Install Docker
        run: |
          sshpass -p '${{ secrets.DEV_PASSWORD }}' ssh -o StrictHostKeyChecking=no \
            student@192.168.1.124 << 'EOF'
          set -e
          # Clear locks again just in case
          sudo rm -f /var/lib/dpkg/lock-frontend
          
          # Install Docker using the official script
          curl -fsSL https://get.docker.com -o get-docker.sh
          sudo sh get-docker.sh
          
          # Setup permissions and directory
          sudo usermod -aG docker student
          sudo mkdir -p /opt/app
          sudo chown student:student /opt/app
          
          echo "=== Verification ==="
          docker --version
          ls -ld /opt/app
          EOF
